package com.change.config.model;

/**
 * Enum representing the type of configuration change.
 */
public enum ConfigChangeType {
    ADD,    // Adding a new configuration
    UPDATE, // Updating an existing configuration
    DELETE  // Deleting an existing configuration
}